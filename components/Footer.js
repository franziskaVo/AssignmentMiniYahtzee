import { Text, View } from "react-native";
import Styles from "../style/Styles";

export default Footer = () => {
    return (
        <View style={Styles.footer}>
            <Text style={Styles.author}>Author: Franziska Vögele</Text>
        </View>

    )
}